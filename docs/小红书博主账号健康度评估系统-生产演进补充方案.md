# 小红书博主账号健康度评估系统 — 生产演进补充方案

| 属性 | 内容 |
|------|------|
| 文档版本 | v1.0 |
| 编写日期 | 2026-06-19 |
| 适用阶段 | MVP 验证后 / 生产化建设 |
| 目标 | 将 MVP 演进为稳定、合规、可扩展的生产平台 |

---

## 1. 演进原则

生产化阶段不只是增加组件，而是围绕稳定性、可信度和合规性补齐能力。

核心原则：

- 先验证数据源，再扩展采集规模。
- 先沉淀评分样本，再优化模型权重。
- 先解决安全和授权，再开放更多使用场景。
- 先保障核心评分链路，再建设报告、监控和数据仓库。

---

## 2. 数据源生产化

### 2.1 数据源准入标准

每个数据源必须完成准入评审后才能进入生产。

| 项目 | 标准 |
|------|------|
| 授权合法性 | 明确授权主体、授权范围、授权有效期 |
| 字段稳定性 | 核心字段连续 14 天无结构性变化 |
| 调用稳定性 | 采集成功率 `>= 95%` |
| 频率限制 | 已配置限流、退避和额度保护 |
| 数据质量 | 缺失率、重复率、异常率可监控 |
| 商用边界 | 确认可用于健康评估和投放辅助决策 |

### 2.2 数据源字段映射

新增 `source_field_mapping` 配置，用于隔离不同平台字段差异。

```json
{
  "source": "pugongying",
  "version": "2026-06",
  "fields": {
    "platform_uid": "creator_id",
    "fans_count": "fans_count",
    "read_count": "view_count",
    "like_count": "like_count",
    "collect_count": "favorite_count",
    "comment_count": "comment_count",
    "share_count": "share_count"
  }
}
```

### 2.3 采集降级策略

| 场景 | 策略 |
|------|------|
| 主数据源不可用 | 切换备选数据源或使用最近一次快照 |
| 单字段缺失 | 标记缺失字段并降低置信度 |
| 接口限流 | 指数退避，延迟采集低优先级账号 |
| 授权失效 | 暂停采集，通知重新授权 |
| 数据结构变化 | 自动熔断该数据源，等待字段映射更新 |

---

## 3. 评分模型生产化

### 3.1 模型版本管理

评分规则、权重、阈值必须带版本。

| 字段 | 说明 |
|------|------|
| `model_version` | 例如 `rules_v1`, `rules_v2`, `hybrid_v1` |
| `weight_config_id` | 使用的权重配置 |
| `benchmark_version` | 使用的赛道基准版本 |
| `calculated_at` | 评分计算时间 |
| `explain_json` | 指标分、扣分项、缺失字段、建议 |

### 3.2 模型校准流程

每月执行一次评分回测：

1. 抽样账号，覆盖不同赛道和粉丝量级。
2. 收集人工评级、投放表现、历史合作效果。
3. 对比系统评分和人工/业务结果。
4. 标记误判样本和原因。
5. 调整阈值、权重或异常惩罚。
6. 发布新的 `model_version`，保留旧版本结果。

### 3.3 Benchmark 分层

不建议所有账号使用同一套阈值。应至少按以下维度建立基准：

| 分层 | 示例 |
|------|------|
| 赛道 | 美妆、母婴、穿搭、美食、家居 |
| 粉丝量级 | `<1k`, `1k-10k`, `10k-50k`, `50k-100k`, `>100k` |
| 内容类型 | 图文、视频、直播 |
| 商业场景 | 品牌投放、MCN 管理、达人筛选 |

生产版中的互动率、爆款率、CPE 应优先与同赛道、同量级账号比较，而不是使用全局固定阈值。

---

## 4. 数据架构演进

### 4.1 PostgreSQL 与 ClickHouse 分工

| 存储 | 职责 |
|------|------|
| PostgreSQL | 账号、授权、配置、评分结果、告警、报告任务 |
| ClickHouse | 账号每日指标、笔记每日指标、趋势分析、赛道基准 |
| Redis | 最新评分缓存、采集去重、任务锁、告警冷却 |
| MinIO/OSS | PDF/Excel 报告、导入文件、原始文件归档 |

### 4.2 数据生命周期

| 数据类型 | 保留策略 |
|------|------|
| 原始 API 响应 | 180 天，敏感字段脱敏或加密 |
| 每日指标明细 | 12-24 个月 |
| 聚合指标 | 24-36 个月 |
| 评分结果 | 36 个月 |
| 审计日志 | 至少 180 天 |
| 授权记录 | 授权有效期结束后按合规要求保留或删除 |

### 4.3 数据质量监控

生产版需要固定监控以下指标：

| 指标 | 告警条件 |
|------|------|
| 采集成功率 | `< 95%` |
| 字段缺失率 | 核心字段 `> 5%` |
| 重复率 | `> 1%` |
| 异常值比例 | `> 3%` |
| 延迟 | 采集完成延迟 `> 2h` |
| 评分覆盖率 | 当日有评分账号 `< 95%` |

---

## 5. 安全生产化

### 5.1 Secret 管理

生产环境禁止在 `docker-compose.yml` 中硬编码密码。

必须改为：

- `.env` 仅用于本地开发。
- 生产环境使用 Vault、云厂商 Secret Manager 或 Kubernetes Secret。
- 所有数据库、Redis、MinIO、Grafana 密码强制轮换。
- API Key 只保存哈希或密文，不保存明文。

### 5.2 服务暴露规则

| 服务 | 生产暴露策略 |
|------|------|
| FastAPI | 只通过 Nginx/API 网关暴露 |
| PostgreSQL | 不暴露公网 |
| ClickHouse | 不暴露公网 |
| Redis | 不暴露公网，启用密码和网络隔离 |
| Airflow | 限制内网/VPN 访问，启用账号认证 |
| Grafana | 启用 SSO 或强密码 |
| MinIO | 控制台不暴露公网，下载使用短期签名 URL |

### 5.3 权限模型

建议角色：

| 角色 | 权限 |
|------|------|
| Admin | 系统配置、用户管理、全部账号 |
| Operator | 账号管理、数据导入、评分查看、告警处理 |
| Analyst | 评分查看、报告导出、趋势分析 |
| Viewer | 只读查看 |
| Auditor | 审计日志、授权记录、合规检查 |

---

## 6. 合规生产化

### 6.1 授权记录

新增授权记录表，记录每个账号的数据使用依据。

```sql
CREATE TABLE account_authorizations (
    id BIGSERIAL PRIMARY KEY,
    account_id BIGINT NOT NULL REFERENCES accounts(id),
    authorization_type VARCHAR(32) NOT NULL,
    authorized_by VARCHAR(128),
    scope JSONB NOT NULL,
    starts_at TIMESTAMP WITH TIME ZONE,
    expires_at TIMESTAMP WITH TIME ZONE,
    revoked_at TIMESTAMP WITH TIME ZONE,
    proof_url VARCHAR(512),
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

### 6.2 撤销授权流程

当账号撤销授权时：

1. 停止新的数据采集。
2. 标记账号状态为 `authorization_revoked`。
3. 按合同或合规要求删除、匿名化或保留必要历史记录。
4. 记录处理结果和操作人。

### 6.3 数据主体请求

生产版应支持：

- 查询已采集字段。
- 导出账号相关数据。
- 删除或匿名化数据。
- 查看授权范围和有效期。

---

## 7. 调度与任务系统

MVP 之后再引入 Airflow。生产版建议 DAG 分层：

| DAG | 频率 | 说明 |
|------|------|------|
| `daily_account_collection` | 每日 | 账号基础信息、粉丝、笔记列表 |
| `daily_note_metrics_collection` | 每日 | 笔记互动数据 |
| `weekly_profile_collection` | 每周 | 粉丝画像、商业合作数据 |
| `etl_quality_check` | 每日 | 清洗、质检、入库 |
| `score_calculation` | 每日 | 批量评分 |
| `alert_evaluation` | 每日/小时 | 告警规则计算 |
| `benchmark_refresh` | 每周/月 | 赛道基准更新 |

任务必须支持：

- 幂等执行。
- 失败重试。
- 断点续跑。
- 任务级告警。
- 数据源级熔断。

---

## 8. API 与报告演进

生产版可增加：

| 功能 | 说明 |
|------|------|
| 批量评分 | 支持账号集合排序和分页 |
| 报告任务 | 异步生成 PDF/Excel |
| 对比分析 | 同赛道、同量级横向对比 |
| 权重配置 | 不同业务场景使用不同权重 |
| 告警规则配置 | 自定义阈值、冷却时间、通知通道 |
| Webhook | 将评分或告警推送到外部系统 |

报告中必须展示：

- 模型版本。
- 数据日期。
- 数据完整度。
- 置信度。
- 缺失字段。
- 不适用的维度。

---

## 9. 生产部署建议

### 9.1 阶段一：单机生产

适用：`< 1,000` 账号。

- FastAPI 2 实例。
- PostgreSQL 单实例 + 每日备份。
- Redis 单实例。
- Nginx 反向代理。
- 本地或云对象存储。

### 9.2 阶段二：数据平台化

适用：`1,000-10,000` 账号。

- 引入 Airflow。
- 引入 ClickHouse。
- 引入 Prometheus/Grafana。
- 报告异步化。
- 告警通知接入企微/飞书/钉钉。

### 9.3 阶段三：高可用

适用：`10,000+` 账号。

- API 水平扩展。
- PostgreSQL 主从或托管高可用。
- ClickHouse 集群。
- Redis 高可用。
- 对象存储使用云服务。
- 备份恢复演练常态化。

---

## 10. 生产验收标准

| 类别 | 标准 |
|------|------|
| 可用性 | 核心 API 月可用性 `>= 99.9%` |
| 性能 | 单账号评分 `< 3s`，100 账号批量评分 `< 30s` |
| 数据 | 当日评分覆盖率 `>= 95%` |
| 采集 | 采集成功率 `>= 95%` |
| 安全 | 无硬编码密钥，核心服务不暴露公网 |
| 合规 | 授权记录完整，支持撤销授权处理 |
| 模型 | 评分规则有版本，支持回测和回滚 |
| 运维 | 有备份、恢复、监控、告警和应急预案 |

---

## 11. 从 MVP 到生产的迁移清单

| 项目 | 动作 |
|------|------|
| 数据库 | 增加授权表、字段映射表、任务记录表 |
| 评分 | 引入模型版本、benchmark 版本、回测流程 |
| 数据源 | 完成蒲公英和第三方平台准入评审 |
| 调度 | 从手动/简单定时迁移到 Airflow |
| 数仓 | 将每日指标同步到 ClickHouse |
| 安全 | 替换硬编码密码，接入 Secret 管理 |
| 合规 | 补齐授权、撤销、删除、导出流程 |
| 报告 | 异步任务化，文件进入对象存储 |
| 监控 | 接入 Prometheus/Grafana 和日志平台 |
